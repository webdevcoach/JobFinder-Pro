import 'package:flutter/material.dart';

class ClosedJobsView extends StatelessWidget {
  const ClosedJobsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
